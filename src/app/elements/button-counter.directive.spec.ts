import { ButtonCounterDirective } from './button-counter.directive';

describe('ButtonCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonCounterDirective();
    expect(directive).toBeTruthy();
  });
});
