import { ButtonErrorDirective } from './button-error.directive';

describe('ButtonErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonErrorDirective();
    expect(directive).toBeTruthy();
  });
});
