import { HelpButtonDirective } from './help-button.directive';

describe('HelpButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new HelpButtonDirective();
    expect(directive).toBeTruthy();
  });
});
